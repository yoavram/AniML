Gemini Prompt Guide for Exercise

1. Load & Clean: "Load reef_survey.csv from Fish.csv. Filter out rows where Weight <= 0. Print the number of rows before and after."

2. Visualize: "Create a boxplot of Weight by Species using seaborn. Use a log scale for the y-axis if needed."

3. Train: "Split the data 80/20. Train a Random Forest Classifier to predict Species. Print the accuracy."

4. Evaluate: "Plot a heatmap of the confusion matrix with labels."
